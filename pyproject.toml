[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-knowledge-engine"
version = "0.1.0"
description = "RAG Knowledge Engine"
authors = [{ name = "dev" }]

[tool.setuptools]
packages = ["app"]